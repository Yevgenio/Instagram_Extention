// Function to start the timer, but only if it is not still running
// function startSessionTimeout() {
//     // Set a new timer for session timeout
//     sessionTimeout.start(sessionTimeoutLength * 1000,
//          () => { sessionTimeoutExpired() }
//         );
// }

// Function to restart the timer, no matter if it has finished or not
// function restartSessionTimeout() {
//     // Set a new timer for session timeout
//     sessionTimeout.restart(sessionTimeoutLength * 1000,
//          () => { sessionTimeoutExpired() }
//         );
// }

// Function to stop and delete the timer
// function cencelSessionTimeout() {
//     sessionTimeout.cancel();
// }
